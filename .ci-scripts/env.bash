REPO_OWNER="redvers"
REPO_NAME="pony-libressl"
GITHUB_USER="redvers"

# Who we are for git
git config --global user.email "red@infect.me"
git config --global user.name "Redvers Davies"
git config --global push.default simple
